# import 방식
# 모듈 import
# 함수 import

# import 모듈이름
# from 모듈이름 import 모듈함수

# from 모듈이름 import 모듈함수

# 같은 폴더안에 있을 때 다른 python파일에 만들어 놓은 함수를 불러와서 쓸 수 있음. 해당 함수만 불러올수도 있고 그 함수가 저장된 파일(모듈)을 통째로 불러오기도 함

import mod
print("mod.add(3,4) = ", mod.add(3, 4))  # 7
